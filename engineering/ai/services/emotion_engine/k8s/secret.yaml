apiVersion: v1
kind: Secret
metadata:
  name: emotion-engine-secrets
  namespace: pao-emotion-engine
  labels:
    app.kubernetes.io/name: emotion-engine
    app.kubernetes.io/part-of: pao-ai
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://postgres:changeme@postgres:5432/emotion_engine"
  REDIS_URL: "redis://redis:6379/0"
  # Add other secrets as needed
  # JWT_SECRET: "changeme"
  # ENCRYPTION_KEY: "changeme"